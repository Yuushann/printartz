# Environment secrets (ignore everything except the committed template)
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build outputs
.next/
dist/
build/

# Prisma generated client (regenerated via `pnpm db:generate` / CI)
packages/db/src/generated/

# TypeScript incremental build cache
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db
