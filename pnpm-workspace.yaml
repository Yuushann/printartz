packages:
  - apps/*
  - packages/*

# Prisma needs its postinstall (engine download + client generate).
# unrs-resolver ships prebuilt binaries; no build needed.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  prisma: true
  unrs-resolver: false
